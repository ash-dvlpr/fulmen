[package]
name = "blob"
version = "0.1.0"
description = "Storage of arbitrary binary data"
crate-type = ["lib"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# Modules

# Logging
thiserror = { version = ">=1.0.60" }

# Other

[features]