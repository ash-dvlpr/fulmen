[package]
name = "blob"
version = "0.1.0"
description = "Storage of arbitrary binary data"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# Modules
fulmen_ptr = { path = "../fulmen_ptr" }

# Logging
thiserror = { workspace = true }

# Other

[features]
default = []